name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping website
        run: |
          curl -I https://pronotesite.onrender.com/site-access
          curl -I https://pronotesite.onrender.com/site-access
